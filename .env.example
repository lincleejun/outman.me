# Cloudflare — API token needs Zone:DNS:Edit on the outman.cc zone, and Workers KV/Scripts edit for the share worker
CF_API_TOKEN=
CF_ZONE_ID=
CLOUDFLARE_ACCOUNT_ID=
# Optional: if set on the worker (wrangler secret put SHARE_TOKEN), POST /share requires "Authorization: Bearer <token>"
SHARE_TOKEN=
