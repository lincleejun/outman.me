# Sharing pages
Whenever an HTML page — report, diff, mockup, chart, dashboard, anything someone else will open — needs to be shared, use the `share-page` skill and hand back the `share.outman.cc` link. Never publish it with the Artifact tool or any third-party host. To find something shared earlier, the same skill has `history.sh`.
